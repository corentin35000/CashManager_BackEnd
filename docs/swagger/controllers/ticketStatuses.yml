paths:
  /ticket-statuses:
    get:
      tags:
        - Ticket status
      description: Get all ticket categories
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TicketStatus_get_return'
